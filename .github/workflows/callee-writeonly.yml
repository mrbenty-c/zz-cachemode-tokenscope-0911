name: callee-writeonly
on: workflow_call
cache-mode: write-only
jobs:
  inner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const t=process.env.ACTIONS_RUNTIME_TOKEN;
            const c=JSON.parse(Buffer.from(t.split('.')[1],'base64url').toString());
            core.info(`CALLEE-WRITEONLY ran. mode=[${process.env.ACTIONS_CACHE_MODE}] ac=${JSON.stringify(c.ac===undefined?'ABSENT':c.ac)}`);
